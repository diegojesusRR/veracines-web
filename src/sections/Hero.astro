<section class="flex flex-col place-items-center gap-4 lg:gap-3">
	<h1 class="sr-only bg-black/30">Veracines</h1>
	<img src="/img/veracines-logo.png" alt="Veracines" class="w-48 lg:w-72" />
	<h2 class=`text-center text-lg text-primary mb-5`>Disfruta de los mejores estrenos de cine durante todo el año en Veracines. Vive con nosotros la experiencia del cine de verano en nuestras terrazas de Vera y Garrucha, así como durante el resto del año en Cine Regio de Vera.</h2>
</section>
<style>
	a::before {
		content: "";
		position: absolute;
		left: 0;
		bottom: -2px;
		width: 100%;
		height: 3px;
		background-color: white;
		transform-origin: right;
		transform: scaleX(0);
		transition: transform 0.2s ease-in-out;
	}

	a:hover::before {
		transform: scaleX(1);
		transform-origin: left;
	}
	@media (prefers-reduced-motion) {
		a::before {
			content: "";
			position: absolute;
			left: 0;
			bottom: -2px;
			width: 100%;
			height: 3px;
			background-color: white;
			transform-origin: right;
			transform: scaleX(0);
			transition: transform 0s;
		}

		a:hover::before {
			transform: scaleX(1);
			transform-origin: left;
		}
	}
</style>
